# Python dependencies: NONE.
#
# This agent is pure Python standard library — urllib handles the Gemini
# API calls, no pip installs required for the agent code itself.
#
# System-level requirements (installed via install.sh, not pip):
#   - Termux "python" package
#   - Termux "git" package
#   - A Gemini API key: https://aistudio.google.com/apikey
#
# This file is kept as a placeholder / convention in case a real
# dependency gets added later.
